> **Prepublication artifact:** This figure uses the archived pre-correction Phase-95 policy or reward set and is included for provenance. Refresh it after final corrected-policy selection.

# Seed Variability

Run `python -B paper/figures/seed-variability/build.py` from the repository
root. It writes only `seed-variability.png` and
`seed-variability.pdf`, not the earlier grouped-summary alternative.

The selected family's seed metrics and historic benchmark table are bundled
in `data/`; that folder's README identifies their original sources.
